# Features Directory

Create one folder per feature (for example: `projects`, `analysis`, `settings`).

Suggested structure per feature:
- request hooks (React Query)
- local state hooks (Context + reducer)
- UI components
- feature service functions that consume frontend ports
